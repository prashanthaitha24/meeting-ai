{
  "id": "devops-secrets",
  "minutes": 30,
  "intro": "Most real-world compromises of delivery systems do not involve clever exploits. They involve a credential that was somewhere it should not have been: an access key in a Git history, a token in a CI log, a password shared in chat. Pipelines and clusters concentrate powerful credentials, which makes them targets. This module covers how to take secrets out of code, replace long-lived keys with short-lived identity, and build security checks into the pipeline so that they run on every change instead of once a year.",
  "objectives": [
    "Identify where secrets leak, and respond correctly when one does",
    "Store and deliver secrets with a secret manager instead of code, images or plain environment files",
    "Replace long-lived cloud keys in CI with OIDC federation and short-lived credentials",
    "Apply least privilege across IAM, Kubernetes RBAC and pipeline permissions",
    "Add automated security gates to a pipeline: secret scanning, SAST, dependency, IaC and policy-as-code checks"
  ],
  "sections": [
    {
      "heading": "Where secrets leak, and what to do when they do",
      "blocks": [
        { "type": "p", "text": "A **secret** is anything that grants access: passwords, API tokens, private keys, database URLs with credentials, signing keys, webhook secrets. They escape through a predictable set of routes." },
        { "type": "list", "items": [
          "**Source control.** A `.env` file or a hard-coded key is committed. Deleting it in a later commit does not help, because it remains in the history and in every clone and fork.",
          "**Container images.** `ENV`, `ARG` and `COPY` persist in image layers, as the Docker module showed.",
          "**Logs and error messages.** A debug line that prints the whole configuration, or a stack trace containing a connection string.",
          "**CI configuration.** Secrets echoed by a script, or exposed to workflows triggered by untrusted pull requests.",
          "**Terraform state and Kubernetes Secrets,** which hold values in plain text or in base64, which is encoding and not encryption.",
          "**People.** Credentials pasted into chat, tickets, wikis and screenshots."
        ]},
        { "type": "p", "text": "When a secret leaks, the order of operations matters, and most teams get it wrong under pressure." },
        { "type": "steps", "items": [
          "**Revoke or rotate the credential immediately.** Assume it has been copied. Public repositories are scraped for keys within minutes.",
          "**Check the audit logs** for use of that credential from the moment of exposure.",
          "**Then** clean up the history, if it is worth doing at all. A rotated secret in the history is harmless; an unrotated one that was deleted is still live.",
          "**Fix the cause**: add scanning, move the secret to a manager, and write it up."
        ]},
        { "type": "code", "lang": "bash", "code": "gitleaks detect --source . --verbose          # scan the working tree and full Git history\ngitleaks protect --staged                     # scan only what is about to be committed\n\n# run it on every commit with the pre-commit framework\npre-commit install" },
        { "type": "callout", "kind": "tip", "text": "Turn on your Git host's built-in secret scanning and **push protection**, which rejects a push that contains a recognisable credential. Blocking the secret before it lands is far cheaper than responding afterwards." }
      ]
    },
    {
      "heading": "Secret managers",
      "blocks": [
        { "type": "p", "text": "A **secret manager** is a service built for this one job: AWS Secrets Manager, Azure Key Vault, Google Secret Manager, or HashiCorp Vault if you run your own. It provides what files and environment files cannot." },
        { "type": "table", "headers": ["Capability", "Why it matters"], "rows": [
          ["Encryption at rest, with keys held in a KMS", "A stolen disk or backup reveals nothing"],
          ["Access controlled by IAM, per secret", "The orders service can read its own database password and nothing else"],
          ["An audit log of every read", "After an incident you know exactly which secrets were accessed, and by whom"],
          ["Versioning and rotation", "Change a credential in one place, on a schedule, without redeploying every consumer"],
          ["Dynamic secrets (Vault, some cloud databases)", "Credentials created on demand for one client and expired automatically"]
        ]},
        { "type": "code", "lang": "bash", "code": "aws secretsmanager create-secret --name shop/prod/orders/db \\\n  --secret-string '{\"username\":\"orders\",\"password\":\"REPLACE_ME\"}'\n\naws secretsmanager get-secret-value --secret-id shop/prod/orders/db \\\n  --query SecretString --output text | jq -r .password" },
        { "type": "p", "text": "In Kubernetes, the **External Secrets Operator** bridges the gap. You commit a manifest that names the entry in the secret manager; the operator, which has a role allowing it to read that entry, creates and refreshes an ordinary Kubernetes Secret. Git holds only a reference, which fits the GitOps module exactly." },
        { "type": "code", "lang": "yaml", "code": "apiVersion: external-secrets.io/v1beta1\nkind: ExternalSecret\nmetadata:\n  name: orders-db\n  namespace: shop\nspec:\n  refreshInterval: 1h\n  secretStoreRef:\n    name: aws-secrets\n    kind: ClusterSecretStore\n  target:\n    name: orders-secrets            # the Kubernetes Secret to create\n  data:\n    - secretKey: DATABASE_PASSWORD\n      remoteRef:\n        key: shop/prod/orders/db\n        property: password" },
        { "type": "p", "text": "Prefer mounting secrets as **files** over environment variables where your application allows it. Environment variables are inherited by child processes and tend to appear in crash dumps and debug endpoints, and a mounted file can be refreshed without restarting the pod." },
        { "type": "callout", "kind": "note", "text": "The operator's API version changes as the project matures. Check the version installed in your cluster with `kubectl api-resources | grep externalsecret` before copying a manifest." }
      ]
    },
    {
      "heading": "OIDC: deploying without stored keys",
      "blocks": [
        { "type": "p", "text": "The traditional way to let CI deploy to the cloud is to create a user, generate an access key, and paste it into the CI system's secrets. That key is long-lived, powerful, held by a third party, and rarely rotated. **OIDC federation** removes it." },
        { "type": "steps", "items": [
          "At the start of a job, the CI platform issues a signed, short-lived **identity token**. Its claims describe the job: which repository, which branch, which environment.",
          "The job presents the token to the cloud provider and asks to assume a role.",
          "The cloud verifies the signature against the CI platform's public keys, and checks the claims against the role's **trust policy**.",
          "If they match, it returns temporary credentials that expire, typically within an hour."
        ]},
        { "type": "code", "lang": "yaml", "caption": "GitHub Actions job using OIDC to assume an AWS role", "code": "jobs:\n  deploy:\n    runs-on: ubuntu-latest\n    environment: production\n    permissions:\n      id-token: write      # allow this job to request an OIDC token\n      contents: read\n    steps:\n      - uses: actions/checkout@v4\n      - uses: aws-actions/configure-aws-credentials@v4\n        with:\n          role-to-assume: arn:aws:iam::123456789012:role/shop-deploy\n          aws-region: eu-west-1\n      - run: aws sts get-caller-identity" },
        { "type": "code", "lang": "json", "caption": "The role's trust policy: only the main branch of one repository may assume it", "code": "{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [\n    {\n      \"Effect\": \"Allow\",\n      \"Principal\": {\n        \"Federated\": \"arn:aws:iam::123456789012:oidc-provider/token.actions.githubusercontent.com\"\n      },\n      \"Action\": \"sts:AssumeRoleWithWebIdentity\",\n      \"Condition\": {\n        \"StringEquals\": {\n          \"token.actions.githubusercontent.com:aud\": \"sts.amazonaws.com\",\n          \"token.actions.githubusercontent.com:sub\": \"repo:acme/shop:ref:refs/heads/main\"\n        }\n      }\n    }\n  ]\n}" },
        { "type": "callout", "kind": "warning", "text": "The `sub` condition is the security boundary. A trust policy that checks only the audience, or uses a wildcard such as `repo:acme/*`, lets any repository or any branch in your organisation, including an unreviewed pull request branch, assume the role." },
        { "type": "p", "text": "The same pattern gives Kubernetes pods cloud access without keys: workload identity on GKE and AKS, IAM roles for service accounts or Pod Identity on EKS. Each pod's service account is mapped to a cloud role, so the orders pod and the payments pod get different permissions." }
      ]
    },
    {
      "heading": "Least privilege everywhere",
      "blocks": [
        { "type": "p", "text": "Least privilege means every identity can do exactly what its job requires and nothing more, so that a compromise of one component is contained there. It applies at every layer you have met in this track." },
        { "type": "list", "items": [
          "**Cloud IAM.** One role per workload, with specific actions on specific resources. Start from zero and add what fails, instead of starting from administrator and meaning to trim later.",
          "**Pipelines.** A `permissions:` block on every workflow, and separate roles for plan and apply, and for staging and production.",
          "**Kubernetes RBAC.** Humans get read access by default, and changes go through Git. Each workload gets its own ServiceAccount.",
          "**Network.** Security groups and Kubernetes NetworkPolicies that allow only the flows a service needs."
        ]},
        { "type": "code", "lang": "yaml", "caption": "A Role that can read pods and their logs in one namespace, bound to a group", "code": "apiVersion: rbac.authorization.k8s.io/v1\nkind: Role\nmetadata:\n  name: pod-reader\n  namespace: shop\nrules:\n  - apiGroups: [\"\"]\n    resources: [\"pods\", \"pods/log\"]\n    verbs: [\"get\", \"list\", \"watch\"]\n---\napiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  name: shop-developers-read\n  namespace: shop\nsubjects:\n  - kind: Group\n    name: shop-developers\n    apiGroup: rbac.authorization.k8s.io\nroleRef:\n  kind: Role\n  name: pod-reader\n  apiGroup: rbac.authorization.k8s.io" },
        { "type": "code", "lang": "bash", "code": "kubectl auth can-i delete pods -n shop --as jane@example.com\nkubectl auth can-i --list -n shop --as system:serviceaccount:shop:orders" },
        { "type": "p", "text": "Be careful with permissions that look harmless. In Kubernetes, the right to read Secrets in a namespace is the right to every credential there, and the right to create pods is, in effect, the right to use any ServiceAccount in that namespace." }
      ]
    },
    {
      "heading": "DevSecOps: security gates in the pipeline",
      "blocks": [
        { "type": "p", "text": "**DevSecOps** means security checks are automated, run on every change, and give feedback in the pull request, while the author still has the context to fix the problem. This is often called **shifting left**. It does not replace security review or penetration testing; it removes the routine findings so that experts can spend their time on the hard ones." },
        { "type": "table", "headers": ["Check", "Finds", "Example tools"], "rows": [
          ["Secret scanning", "Credentials in code and history", "gitleaks, host push protection"],
          ["SAST (static analysis)", "Injection, unsafe deserialisation, weak cryptography in your code", "Semgrep, CodeQL"],
          ["SCA (dependency scanning)", "Known CVEs in libraries", "Trivy, Grype, Dependabot alerts"],
          ["Image scanning", "CVEs in OS packages, running as root", "Trivy, Grype"],
          ["IaC scanning", "Public buckets, open security groups, unencrypted disks", "Checkov, Trivy config, tfsec"],
          ["DAST (dynamic analysis)", "Flaws visible in a running application", "OWASP ZAP"]
        ]},
        { "type": "p", "text": "**Policy as code** takes this further: organisational rules become versioned, testable code that is enforced automatically. **Open Policy Agent** with its Rego language, and **Kyverno** with YAML policies, are the common tools. The same policy can run in CI against manifests, where it advises, and as a Kubernetes **admission controller**, where it blocks." },
        { "type": "code", "lang": "yaml", "caption": "A Kyverno policy that rejects pods which may run as root", "code": "apiVersion: kyverno.io/v1\nkind: ClusterPolicy\nmetadata:\n  name: require-run-as-non-root\nspec:\n  validationFailureAction: Enforce\n  rules:\n    - name: check-run-as-non-root\n      match:\n        any:\n          - resources:\n              kinds: [Pod]\n      validate:\n        message: \"Containers must set securityContext.runAsNonRoot to true.\"\n        pattern:\n          spec:\n            containers:\n              - securityContext:\n                  runAsNonRoot: true" },
        { "type": "p", "text": "Introduce gates gradually. Start every new check in report-only or `Audit` mode, fix the existing findings, and only then switch it to blocking for **new** violations. A gate that fails every build on day one will be disabled on day two, and a disabled check protects nothing." }
      ]
    }
  ],
  "practice": {
    "title": "Remove a secret, remove a key, add a gate",
    "steps": [
      "In a scratch repository, commit a fake API key, then delete it in a second commit. Run `gitleaks detect` and confirm that it still finds the key in history. Write down the correct response order for a real leak.",
      "Install gitleaks as a pre-commit hook and confirm that it now blocks the commit. Enable secret scanning and push protection in your Git host's settings.",
      "Store a database password in your cloud's secret manager, or run Vault in dev mode locally. Grant one role read access to that single secret and read it from the CLI.",
      "Replace a stored cloud access key in a GitHub Actions workflow with OIDC: create the identity provider and a role whose trust policy pins your repository and the `main` branch, then prove `aws sts get-caller-identity` works with no stored secret.",
      "Try to assume the same role from a different branch and confirm that it is denied. Then delete the old access key.",
      "Create a namespaced read-only Role and RoleBinding in your cluster, and test it with `kubectl auth can-i`.",
      "Add Trivy config scanning or Checkov to your Terraform pipeline in report-only mode. Fix one finding, then make the job fail on HIGH and CRITICAL findings."
    ]
  },
  "cheatsheet": {
    "focus": [
      "A leaked secret is compromised. Rotate first, investigate second, clean history last.",
      "Secrets never belong in Git, images, logs or Terraform variables committed to a repository. They belong in a secret manager.",
      "base64 is encoding, not encryption. Kubernetes Secrets need RBAC and encryption at rest.",
      "Replace long-lived CI keys with OIDC federation, and pin the trust policy to a repository and a branch or environment.",
      "Least privilege at every layer: cloud IAM, pipeline token, Kubernetes RBAC, network policy.",
      "Shift left: automated security checks on every pull request, while the author still has context.",
      "Roll out gates in audit mode first, then block new violations."
    ],
    "groups": [
      {
        "title": "Leak response, in order",
        "items": [
          { "syntax": "1. revoke / rotate", "desc": "Immediately; assume the secret has been copied" },
          { "syntax": "2. audit", "desc": "Search access logs for use since the exposure" },
          { "syntax": "3. contain", "desc": "Rotate anything the credential could reach" },
          { "syntax": "4. clean up", "desc": "Remove from history only if worthwhile; rotation made it harmless" },
          { "syntax": "5. prevent", "desc": "Add scanning, move to a manager, record what was learned" }
        ]
      },
      {
        "title": "Finding secrets",
        "items": [
          { "syntax": "gitleaks detect --source .", "desc": "Scan the working tree and full history" },
          { "syntax": "gitleaks protect --staged", "desc": "Scan staged changes; use in a pre-commit hook" },
          { "syntax": "pre-commit install", "desc": "Activate the hooks in `.pre-commit-config.yaml`" },
          { "syntax": "git log -p -S 'AKIA' --all", "desc": "Find commits that added or removed a string" },
          { "syntax": "push protection (Git host setting)", "desc": "Reject pushes that contain known credential formats" }
        ]
      },
      {
        "title": "Secret managers and Kubernetes",
        "items": [
          { "syntax": "aws secretsmanager get-secret-value --secret-id NAME", "desc": "Read a secret (every read is audited)" },
          { "syntax": "vault kv put secret/PATH key=value", "desc": "Write to Vault's key-value engine" },
          { "syntax": "vault kv get -field=key secret/PATH", "desc": "Read a single field" },
          { "syntax": "kind: ExternalSecret", "desc": "Sync a manager entry into a Kubernetes Secret" },
          { "syntax": "kubectl get secret NAME -o jsonpath='{.data.KEY}' | base64 -d", "desc": "Decode a Secret, which shows how little base64 protects" },
          { "syntax": "volumeMounts + secret volume", "desc": "Deliver secrets as files, refreshable without a restart" }
        ]
      },
      {
        "title": "OIDC federation",
        "items": [
          { "syntax": "permissions: { id-token: write }", "desc": "Let a GitHub Actions job request an OIDC token" },
          { "syntax": "sts:AssumeRoleWithWebIdentity", "desc": "The AWS action that exchanges the token for credentials" },
          { "syntax": "sub = repo:ORG/REPO:ref:refs/heads/main", "desc": "Claim to pin in the trust policy: repository and branch" },
          { "syntax": "sub = repo:ORG/REPO:environment:production", "desc": "Alternative claim: pin to a protected environment" },
          { "syntax": "aud = sts.amazonaws.com", "desc": "Audience claim expected by AWS" }
        ]
      },
      {
        "title": "Least privilege checks",
        "items": [
          { "syntax": "kubectl auth can-i VERB RESOURCE -n NS --as USER", "desc": "Test a permission as another identity" },
          { "syntax": "kubectl auth can-i --list -n NS", "desc": "Everything the current identity may do" },
          { "syntax": "Role / RoleBinding", "desc": "Permissions within one namespace" },
          { "syntax": "ClusterRole / ClusterRoleBinding", "desc": "Cluster-wide permissions; grant sparingly" },
          { "syntax": "automountServiceAccountToken: false", "desc": "Do not give a pod an API token it does not need" }
        ]
      },
      {
        "title": "Pipeline gates",
        "items": [
          { "syntax": "semgrep scan --config auto", "desc": "Static analysis of your code" },
          { "syntax": "trivy fs --scanners vuln,secret .", "desc": "Dependencies and secrets in a repository" },
          { "syntax": "trivy config .", "desc": "Misconfigurations in Terraform, Kubernetes, Dockerfiles" },
          { "syntax": "checkov -d .", "desc": "IaC policy checks" },
          { "syntax": "conftest test FILE", "desc": "Evaluate OPA/Rego policies against config files" },
          { "syntax": "validationFailureAction: Audit | Enforce", "desc": "Kyverno: report only, or block" }
        ]
      }
    ],
    "pitfalls": [
      "Deleting a leaked key from the repository and considering the matter closed, without rotating it.",
      "An OIDC trust policy with a wildcard subject, so that any branch or repository can assume the deploy role.",
      "Believing that Kubernetes Secrets are encrypted because the values look scrambled.",
      "Granting `cluster-admin` or cloud administrator to a pipeline because working out the real permissions was tedious.",
      "Printing the environment or full configuration in debug logs.",
      "Switching on every scanner in blocking mode at once, then switching them all off a week later."
    ]
  },
  "quiz": [
    {
      "q": "A developer pushed a commit containing a live cloud access key to a public repository, then removed it in a follow-up commit five minutes later. What must happen first?",
      "options": [
        "Revoke the key immediately and issue a new one",
        "Rewrite the Git history so that the key no longer appears",
        "Make the repository private until the review is finished",
        "Ask the developer to confirm that nobody cloned the repository"
      ],
      "answer": 0,
      "explain": "The key must be treated as copied, because public repositories are scraped continuously. Only revocation makes it harmless. History rewriting and privacy changes do nothing about copies already taken."
    },
    {
      "q": "What is the main advantage of OIDC federation over storing a cloud access key in CI secrets?",
      "options": [
        "No long-lived credential exists; each job gets temporary credentials tied to its identity",
        "The access key is encrypted with a stronger algorithm inside the CI system",
        "Deployments run faster because no authentication round trip is needed",
        "The CI platform takes over responsibility for cloud IAM policies"
      ],
      "answer": 0,
      "explain": "With OIDC there is nothing durable to steal or rotate. The cloud issues short-lived credentials only when the token's claims, such as repository and branch, satisfy the role's trust policy."
    },
    {
      "q": "A role's trust policy for GitHub OIDC checks only that the audience is `sts.amazonaws.com`. Why is that dangerous?",
      "options": [
        "Any repository on the platform could assume the role, since the subject is not restricted",
        "The role's credentials would never expire without a subject condition",
        "The audience claim is not signed, so it can be forged by anyone",
        "AWS rejects all tokens that do not include a subject condition"
      ],
      "answer": 0,
      "explain": "The audience is the same for every GitHub Actions workflow. The `sub` claim identifies the repository and branch or environment, and pinning it is what restricts the role to your pipeline."
    },
    {
      "q": "Which statement about Kubernetes Secrets is correct?",
      "options": [
        "Values are base64-encoded, so anyone who can read the object can recover them",
        "Values are encrypted with a key that only the pod can access",
        "Secrets cannot be read through the API once they have been created",
        "Secrets are automatically synchronised with the cloud secret manager"
      ],
      "answer": 0,
      "explain": "base64 is reversible encoding. Protection comes from RBAC limiting who can read Secrets, encryption at rest in etcd, and sourcing values from an external manager."
    },
    {
      "q": "You are introducing an IaC security scanner to a repository with 200 existing findings. Which rollout is most likely to succeed?",
      "options": [
        "Start in report-only mode, fix the backlog, then block only new violations",
        "Enable it as a blocking gate at once so that problems cannot be ignored",
        "Run it manually once a quarter and email the results to the team",
        "Add all 200 findings to a permanent ignore list and enable blocking"
      ],
      "answer": 0,
      "explain": "A gate that fails every build gets bypassed or removed. Reporting first builds trust and clears the backlog, after which blocking new violations keeps the baseline from getting worse."
    }
  ]
}
