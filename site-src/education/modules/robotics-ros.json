{
  "id": "robotics-ros",
  "minutes": 35,
  "intro": "ROS 2 — the Robot Operating System — is the framework nearly every serious robot uses to organise its software. It is not an operating system; it is a way to split a robot's brain into small programs that talk to each other over a common bus. This lesson explains the handful of concepts that unlock ROS 2: nodes, topics, messages, services and the tools to inspect a running system. You do not need ROS to blink an LED, but the moment your robot has several sensors, a planner and motors all running at once, ROS is what keeps it sane.",
  "objectives": [
    "Explain what ROS 2 is and why robots use it",
    "Describe nodes, topics, and the publish/subscribe model",
    "Tell topics apart from services and know when to use each",
    "Use the core command-line tools to inspect a running ROS 2 system"
  ],
  "sections": [
    {
      "heading": "Why a robot needs a framework",
      "blocks": [
        {"type": "p", "text": "A real robot runs many things at once: a camera driver, a lidar driver, a program that finds obstacles, one that plans a path, one that drives the motors, maybe a battery monitor. Cram all of that into one giant program and it becomes impossible to test, reuse, or debug — change the camera and you risk breaking the motors. **ROS 2** solves this by letting you write each piece as a small, independent program and giving them a standard way to pass data to each other. The payoff is huge: you can develop and test each part alone, swap one out without touching the others, reuse drivers other people wrote, and run the pieces across several computers. That modularity is why ROS 2 is the near-universal choice for non-trivial robots, in research and increasingly in industry."}
      ]
    },
    {
      "heading": "Nodes and topics",
      "blocks": [
        {"type": "p", "text": "The two core ideas are nodes and topics. A **node** is one small program doing one job — a camera node, a motor node, an obstacle-detector node. Nodes do not call each other directly; instead they communicate through **topics**, which are named channels carrying a stream of **messages**. A node **publishes** to a topic (the camera node publishes images to `/camera/image`) and any number of other nodes **subscribe** to receive them. This is the **publish/subscribe** model: publishers and subscribers do not know about each other, they only share the topic name and message type. Add a second node that also wants camera images and it just subscribes to the same topic — nothing else changes. This loose coupling is what makes ROS systems so easy to extend."},
        {"type": "callout", "kind": "note", "text": "The mental model: nodes are the workers, topics are the conveyor belts between them, and messages are the items on the belt. A publisher drops items on a belt; every subscriber to that belt picks them up. No worker needs to know who else is listening."}
      ]
    },
    {
      "heading": "A publisher and a subscriber",
      "blocks": [
        {"type": "p", "text": "ROS 2 nodes are usually written in Python (with `rclpy`) or C++. Here is the shape of a Python node that publishes a number every half-second, and a second one that subscribes and prints it. You do not need to memorise the API — notice the structure: create a node, make a publisher or subscription bound to a topic name and message type, and let ROS spin the node so callbacks fire."},
        {"type": "code", "lang": "python", "code": "import rclpy\nfrom rclpy.node import Node\nfrom std_msgs.msg import Int32\n\nclass Talker(Node):\n    def __init__(self):\n        super().__init__('talker')\n        self.pub = self.create_publisher(Int32, 'count', 10)\n        self.n = 0\n        self.create_timer(0.5, self.tick)   # call tick() every 0.5s\n\n    def tick(self):\n        msg = Int32()\n        msg.data = self.n\n        self.pub.publish(msg)               # send it on the 'count' topic\n        self.n += 1\n\ndef main():\n    rclpy.init()\n    rclpy.spin(Talker())                    # process callbacks forever\n    rclpy.shutdown()"}
      ]
    },
    {
      "heading": "Topics vs services vs actions",
      "blocks": [
        {"type": "p", "text": "Topics are for continuous streams, but not every interaction is a stream. ROS 2 gives you three patterns, and choosing the right one matters. A **topic** is a one-way stream of data with no reply — sensor readings, motor commands, anything published continuously. A **service** is a request/response call — you ask a question and wait for one answer, like 'reset the odometry' or 'what is the current map'; use it for quick, occasional commands that need a reply. An **action** is for long-running goals that report progress and can be cancelled — 'navigate to this room', which takes seconds and streams feedback along the way. The rule of thumb: streaming data → topic; quick call-and-answer → service; a goal that takes time and you want progress on → action."},
        {"type": "list", "items": [
          "Topic → continuous one-way stream (sensor data, velocity commands).",
          "Service → request/response, quick and occasional (reset, query a value).",
          "Action → long-running goal with progress and cancellation (navigate somewhere)."
        ]}
      ]
    },
    {
      "heading": "Seeing inside a running system",
      "blocks": [
        {"type": "p", "text": "A great strength of ROS 2 is that you can inspect a live robot from the command line without changing any code. The `ros2` tool lets you list what is running and even inject or watch data. This is how you debug: is the camera node actually publishing? Echo its topic. Is the motor node receiving commands? Check the topic's subscribers. These commands turn a mysterious 'the robot isn't moving' into a quick diagnosis of exactly which link in the chain is broken."},
        {"type": "code", "lang": "bash", "code": "ros2 node list                 # every node currently running\nros2 topic list                # every active topic\nros2 topic echo /count         # print messages arriving on a topic\nros2 topic hz /camera/image    # measure a topic's publish rate\nros2 topic info /count         # see a topic's type and how many pubs/subs\nros2 service list              # every available service"}
      ]
    }
  ],
  "practice": {
    "title": "Run your first ROS 2 graph",
    "steps": [
      "Install ROS 2 Humble (or run it in the provided Docker image) and source the setup file.",
      "Run the built-in demo talker and listener nodes in two terminals and watch them communicate.",
      "In a third terminal, run 'ros2 topic list' and find the topic the talker publishes on.",
      "Run 'ros2 topic echo' on that topic and confirm you see the same messages the listener prints.",
      "Run 'ros2 topic hz' on the topic to measure how many messages per second it carries."
    ]
  },
  "cheatsheet": {
    "focus": [
      "ROS 2 splits a robot's software into small programs (nodes) that share data.",
      "Nodes communicate over named topics using the publish/subscribe model.",
      "Publishers and subscribers know only the topic name and message type, not each other.",
      "Topic = stream; service = request/response; action = long goal with progress.",
      "rclpy (Python) and rclcpp (C++) are the client libraries for writing nodes.",
      "The ros2 command-line tools inspect a live system without changing code."
    ],
    "groups": [
      {"title": "Core concepts", "items": [
        {"syntax": "node", "desc": "one small program doing one job"},
        {"syntax": "topic", "desc": "named channel carrying a message stream"},
        {"syntax": "message", "desc": "a typed data item on a topic"},
        {"syntax": "publish / subscribe", "desc": "send to / receive from a topic"}
      ]},
      {"title": "Communication patterns", "items": [
        {"syntax": "topic", "desc": "one-way continuous stream, no reply"},
        {"syntax": "service", "desc": "request/response, quick and occasional"},
        {"syntax": "action", "desc": "long goal with feedback and cancel"},
        {"syntax": "rclpy / rclcpp", "desc": "Python / C++ node libraries"}
      ]},
      {"title": "Writing a node", "items": [
        {"syntax": "rclpy.init()", "desc": "start the ROS client library"},
        {"syntax": "create_publisher(type, name, q)", "desc": "make a publisher on a topic"},
        {"syntax": "create_subscription(...)", "desc": "receive messages via a callback"},
        {"syntax": "rclpy.spin(node)", "desc": "process callbacks until shutdown"}
      ]},
      {"title": "Inspecting live", "items": [
        {"syntax": "ros2 node list", "desc": "running nodes"},
        {"syntax": "ros2 topic list", "desc": "active topics"},
        {"syntax": "ros2 topic echo <t>", "desc": "print a topic's messages"},
        {"syntax": "ros2 topic hz <t>", "desc": "measure publish rate"}
      ]}
    ],
    "pitfalls": [
      "Writing one giant program instead of small nodes, losing all the modularity ROS gives.",
      "Using a topic for a request that needs an answer — that is what a service is for.",
      "Publishing and subscribing with mismatched message types, so nothing is received.",
      "Forgetting to spin the node, so callbacks never fire and it looks dead.",
      "Debugging blind instead of using ros2 topic echo to see what is actually flowing."
    ]
  },
  "quiz": [
    {
      "q": "In ROS 2, what is a node?",
      "options": [
        "A single small program that does one job",
        "The main battery of the robot",
        "A physical connector on the board",
        "The name of the robot's Wi-Fi network"
      ],
      "answer": 0,
      "explain": "A node is one small independent program (a camera driver, a motor controller); nodes combine to form the robot's software."
    },
    {
      "q": "How do two ROS 2 nodes exchange a continuous stream of sensor data?",
      "options": [
        "One node calls a function inside the other directly",
        "One publishes to a topic and the other subscribes to it",
        "They share a global variable in memory",
        "They email the data to each other"
      ],
      "answer": 1,
      "explain": "Streaming data flows over a named topic: a publisher sends messages and any subscriber to that topic receives them, without the nodes knowing each other."
    },
    {
      "q": "You need a node to answer a quick one-off request like 'reset the odometry'. Which pattern fits?",
      "options": [
        "A topic",
        "A service",
        "A louder message",
        "A separate battery"
      ],
      "answer": 1,
      "explain": "A service is request/response — ask once and get one answer — which suits quick occasional commands; topics are for continuous streams."
    },
    {
      "q": "Which pattern is designed for a long-running goal that reports progress and can be cancelled?",
      "options": [
        "A topic",
        "A service",
        "An action",
        "A message type"
      ],
      "answer": 2,
      "explain": "Actions handle goals that take time, such as 'navigate to a room', streaming feedback along the way and allowing cancellation."
    },
    {
      "q": "The robot won't move and you suspect the command topic. What lets you check it live?",
      "options": [
        "Rewriting the motor node from scratch",
        "Running 'ros2 topic echo' on that topic to see the messages",
        "Restarting the whole computer",
        "Replacing the motors"
      ],
      "answer": 1,
      "explain": "'ros2 topic echo' prints the messages flowing on a topic, so you can see immediately whether commands are being published as expected."
    }
  ]
}
