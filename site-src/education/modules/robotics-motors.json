{
  "id": "robotics-motors",
  "minutes": 34,
  "intro": "Motors are how a robot acts on the world — everything the robot decides to do finally becomes a motor turning. This lesson covers the three motor types you will meet first: DC motors for wheels, servos for precise angles, and steppers for exact positioning. You will learn why a microcontroller cannot drive a motor directly, what a motor driver does, and how to control speed and direction in code. Get this right and your robot moves the way you intend instead of stalling or resetting.",
  "objectives": [
    "Tell a DC motor, a servo, and a stepper apart and pick the right one",
    "Explain why a motor needs a driver between it and the microcontroller",
    "Control motor speed with PWM and direction with an H-bridge",
    "Drive a hobby servo to a commanded angle in code"
  ],
  "sections": [
    {
      "heading": "Three kinds of motor",
      "blocks": [
        {"type": "p", "text": "Most first robots use one of three motors, and choosing the right one saves a lot of grief. A **DC motor** spins continuously when powered; speed follows voltage and direction follows polarity. It is what drives wheels — cheap, fast, but with no built-in sense of position (you add an encoder for that). A **servo** is a small geared motor with built-in electronics that holds a commanded *angle*, typically 0 to 180 degrees; you tell it 'go to 90 degrees' and it goes and holds. It is perfect for a robot arm joint, a steering linkage, or a sensor pan. A **stepper** moves in precise fixed steps and can hold an exact position without feedback, which makes it the choice for 3D printers and anything needing repeatable precision, at the cost of speed and more complex driving."},
        {"type": "list", "items": [
          "DC motor → wheels and continuous motion; needs a driver, add an encoder for position.",
          "Servo → a held angle 0–180°; great for arm joints and steering.",
          "Stepper → exact repeatable steps; precise but slower and needs a stepper driver."
        ]}
      ]
    },
    {
      "heading": "Why you cannot drive a motor from a pin",
      "blocks": [
        {"type": "p", "text": "A microcontroller pin can supply only a tiny current — around 20 to 40 milliamps. A motor draws hundreds of milliamps to several amps, especially at the moment it starts or stalls. Wire a motor straight to a pin and you will destroy the pin, the board, or both, and a spinning motor also generates voltage spikes back into the circuit. So a motor is **always** driven through a separate circuit — a motor driver — that takes a small control signal from the microcontroller and switches the large motor current from a separate power supply. The microcontroller commands; the driver does the heavy lifting."},
        {"type": "callout", "kind": "warning", "text": "Never connect a DC motor directly to an Arduino pin, and never power motors from the board's 5V pin. Use a driver and a separate motor supply, and join the two grounds so they share a reference. This single rule prevents most fried boards."}
      ]
    },
    {
      "heading": "The H-bridge: speed and direction",
      "blocks": [
        {"type": "p", "text": "The standard DC-motor driver is an **H-bridge** — four switches arranged so they can send current through the motor in either direction, which is how you reverse it. Boards like the L298N or the smaller TB6612 or DRV8833 give you two control pins to set direction and one PWM pin to set speed per motor. You set the two direction pins to one combination for forward, the opposite for reverse, both low to coast, and you feed a PWM value to the enable pin for speed. Two motors driven this way is a complete differential-drive robot: both forward to go straight, one faster to turn, opposite directions to spin in place."},
        {"type": "code", "lang": "cpp", "code": "// Drive one DC motor through an H-bridge (e.g. L298N).\nconst int IN1 = 7, IN2 = 8, ENA = 9;  // ENA must be a PWM (~) pin\n\nvoid setup() {\n  pinMode(IN1, OUTPUT);\n  pinMode(IN2, OUTPUT);\n  pinMode(ENA, OUTPUT);\n}\n\nvoid drive(int speed, bool forward) {\n  digitalWrite(IN1, forward ? HIGH : LOW);\n  digitalWrite(IN2, forward ? LOW : HIGH);\n  analogWrite(ENA, speed);   // 0-255 sets speed via PWM\n}\n\nvoid loop() {\n  drive(180, true);   delay(1000);  // forward, ~70% speed\n  drive(0, true);     delay(500);   // stop\n  drive(180, false);  delay(1000);  // reverse\n  drive(0, true);     delay(500);\n}"}
      ]
    },
    {
      "heading": "Servos: commanding an angle",
      "blocks": [
        {"type": "p", "text": "A servo is easier: it has three wires — power, ground, and one signal — and you command an angle with the Arduino `Servo` library. Under the hood the library sends a pulse every 20 milliseconds whose width (roughly 1 to 2 milliseconds) encodes the angle, but you never deal with that directly; you just call `write(angle)`. A small servo can run off the board for testing, but anything under load needs its own supply, with grounds joined, exactly like a DC motor. Servos are the fastest way to give a robot precise, repeatable joints."},
        {"type": "code", "lang": "cpp", "code": "#include <Servo.h>\nServo joint;\n\nvoid setup() {\n  joint.attach(9);   // signal wire on pin 9\n}\n\nvoid loop() {\n  joint.write(0);     delay(600);   // go to 0 degrees\n  joint.write(90);    delay(600);   // centre\n  joint.write(180);   delay(600);   // full travel\n}"},
        {"type": "callout", "kind": "tip", "text": "If a servo jitters or the board resets when it moves, it is a power problem: the servo's stall current is dipping the supply. Give the servo its own battery or a separate regulator, join the grounds, and the jitter usually stops."}
      ]
    },
    {
      "heading": "Making two motors go straight",
      "blocks": [
        {"type": "p", "text": "A practical surprise: command two DC motors to the same speed and the robot still curves, because no two motors are identical and one wheel grips more. There are two fixes. The cheap one is **trim** — nudge one motor's PWM up by a fixed amount you find by trial until it tracks straight, which works but drifts as the battery drains. The proper one is **closed-loop control** with encoders: measure each wheel's actual speed and adjust its PWM to match a target, so the robot corrects itself. You will meet that control idea in depth in the control lesson; for now, know that 'drive straight' is a control problem, not just equal PWM."}
      ]
    }
  ],
  "practice": {
    "title": "Drive a two-wheel robot",
    "steps": [
      "Wire two DC motors to an H-bridge with a separate battery, joining the battery ground to the board ground.",
      "Write a drive(speed, forward) helper for each motor using two direction pins and a PWM enable pin.",
      "Make the robot go forward for two seconds, stop, then reverse, and confirm both directions work.",
      "Spin in place by driving the two motors in opposite directions.",
      "Notice any curve when going 'straight' and add a small trim offset to one motor's PWM until it tracks straighter."
    ]
  },
  "cheatsheet": {
    "focus": [
      "DC motor = continuous spin for wheels; servo = a held angle; stepper = exact steps.",
      "A pin cannot power a motor — always use a driver and a separate motor supply.",
      "An H-bridge sets direction with two pins and speed with a PWM enable pin.",
      "Join the motor supply ground to the board ground so they share a reference.",
      "Servos take power/ground/signal and a write(angle) call via the Servo library.",
      "Equal PWM does not mean straight — that is a closed-loop control problem."
    ],
    "groups": [
      {"title": "Motor types", "items": [
        {"syntax": "DC motor", "desc": "continuous spin; speed by voltage, direction by polarity"},
        {"syntax": "Servo", "desc": "holds a commanded angle 0–180°"},
        {"syntax": "Stepper", "desc": "moves in exact repeatable steps"},
        {"syntax": "Encoder + DC", "desc": "adds position sense to a DC motor"}
      ]},
      {"title": "Driving a DC motor", "items": [
        {"syntax": "H-bridge", "desc": "four switches; reverses current for direction"},
        {"syntax": "IN1 / IN2", "desc": "two pins set forward, reverse, or coast"},
        {"syntax": "analogWrite(ENA, 0-255)", "desc": "PWM on the enable pin sets speed"},
        {"syntax": "L298N / TB6612", "desc": "common dual H-bridge driver boards"}
      ]},
      {"title": "Driving a servo", "items": [
        {"syntax": "#include <Servo.h>", "desc": "the Arduino servo library"},
        {"syntax": "servo.attach(pin)", "desc": "bind the servo to its signal pin"},
        {"syntax": "servo.write(angle)", "desc": "command an angle in degrees"},
        {"syntax": "~20ms pulse", "desc": "the timing the library sends for you"}
      ]},
      {"title": "Power & grounds", "items": [
        {"syntax": "Separate motor supply", "desc": "motors never run off the board's 5V"},
        {"syntax": "Common ground", "desc": "join board and motor grounds together"},
        {"syntax": "Stall current", "desc": "the big current spike at start or when blocked"}
      ]}
    ],
    "pitfalls": [
      "Wiring a DC motor straight to a pin, which destroys the pin or the board.",
      "Powering motors or a loaded servo from the board's 5V, causing resets and jitter.",
      "Forgetting to join the motor supply ground to the board ground, so signals float.",
      "Assuming equal PWM makes the robot go straight — it curves without trim or feedback.",
      "Using a non-PWM pin for the H-bridge enable, so speed control does nothing."
    ]
  },
  "quiz": [
    {
      "q": "Which motor is the natural choice for a robot arm joint that must hold a set angle?",
      "options": [
        "A plain DC motor",
        "A servo",
        "A vibration motor",
        "An ungeared spinner"
      ],
      "answer": 1,
      "explain": "A servo takes a commanded angle and holds it, which is exactly what an arm joint or steering linkage needs."
    },
    {
      "q": "Why must a motor be driven through a driver rather than straight from a pin?",
      "options": [
        "A pin's current is far too small and the pin would be destroyed",
        "Drivers are required to make the motor run quieter and cooler",
        "Pins are only able to send low-frequency analog control signals",
        "Without a driver the motor would spin far too slowly to be useful"
      ],
      "answer": 0,
      "explain": "A pin supplies only tens of milliamps while a motor draws hundreds of milliamps to amps; a driver switches the large current from a separate supply."
    },
    {
      "q": "In an H-bridge, what sets the direction a DC motor spins?",
      "options": [
        "The value of the PWM signal sent to the enable pin each cycle",
        "The combination of the two direction pins",
        "The overall length of the motor's power wires",
        "The total charge capacity of the battery pack"
      ],
      "answer": 1,
      "explain": "The two direction pins choose which way current flows through the motor; the PWM enable pin sets the speed, not the direction."
    },
    {
      "q": "Your board resets whenever a servo moves under load. What is the most likely cause?",
      "options": [
        "There is a subtle bug in the write() call somewhere",
        "The servo's current draw is sagging the shared supply",
        "The signal wire to the servo is slightly too short",
        "The installed servo library version is out of date"
      ],
      "answer": 1,
      "explain": "A loaded servo's current spike sags the supply and browns out the board; give the servo its own supply and join the grounds."
    },
    {
      "q": "You command two DC motors to the same PWM but the robot curves. Why?",
      "options": [
        "PWM values can never actually be made equal on two pins",
        "No two motors are identical, so equal PWM gives unequal speed",
        "An H-bridge is only able to drive one of the motors at a time",
        "Any curving at all means one of the two motors is broken"
      ],
      "answer": 1,
      "explain": "Manufacturing and grip differences make equal PWM produce unequal motion; a trim offset or encoder-based closed loop is needed to go straight."
    }
  ]
}
