{
  "id": "de-infra",
  "minutes": 35,
  "intro": "Everything in this track runs on infrastructure somebody has to provision, secure, pay for and change without breaking the nightly run: buckets and warehouses, an orchestrator, compute for Spark and dbt, secrets, networking, identities. Treated as click-ops, a data platform becomes a snowflake nobody can rebuild and a bill nobody can explain. Treated as software — infrastructure in Terraform, pipelines and models deployed through CI, environments that mirror each other, cost attributed to owners — it becomes something a small team can run for a large organisation. This module applies the DevOps track to data: the platform components and their boundaries, IaC for data resources, CI/CD for pipelines and dbt, environments and promotion, and cost control.",
  "objectives": [
    "Describe the components of a data platform and the boundaries between them",
    "Provision data infrastructure with Terraform: storage, warehouse objects, orchestrator, identities, secrets",
    "Build CI/CD for pipelines and dbt projects with environment promotion",
    "Design development, staging and production environments for data without copying production data everywhere",
    "Attribute and control cost across storage, compute and orchestration"
  ],
  "sections": [
    {
      "heading": "The components and their boundaries",
      "blocks": [
        {"type": "p", "text": "A data platform has a small number of moving parts, and most operational pain comes from unclear boundaries between them. **Ingestion** (connectors, CDC, API pullers) lands raw data. **Storage** (object storage with a table format, or the warehouse's own storage) holds every layer. **Compute** runs transformations: the warehouse engine for SQL, Spark or single-node engines for code. **Orchestration** schedules and records runs. **Transformation code** (dbt, pipelines) lives in repositories. **Governance and observability** (catalog, quality, lineage, metrics) sit across all of it. **Serving** (BI, reverse ETL, feature stores, APIs) reads the outputs. Each is a separate concern with its own identity, its own cost line and its own deploy path."},
        {"type": "table", "headers": ["Component", "Managed options", "What you still own"], "rows": [
          ["Storage and tables", "S3/GCS/Blob + Iceberg catalog; warehouse-native", "Layout, retention, access policies"],
          ["SQL compute", "BigQuery, Snowflake, Redshift, Databricks SQL", "Sizing, isolation, cost caps"],
          ["Code compute", "EMR, Dataproc, Databricks, Glue; containers on Kubernetes", "Job packaging, dependencies, scaling limits"],
          ["Orchestration", "Managed Airflow (MWAA, Composer, Astronomer), Dagster Cloud, Prefect", "DAG code, alerting, secrets, upgrades"],
          ["Ingestion", "Fivetran-style connectors, managed CDC, Kafka Connect", "Contracts, monitoring, cost per row"]
        ]},
        {"type": "callout", "kind": "note", "text": "Prefer managed services for the stateful, hard-to-operate parts (warehouse, Kafka, orchestrator control plane) and own the parts that encode your business (models, pipelines, contracts, policies). Operating Kafka yourself is a job; writing good models is your job."}
      ]
    },
    {
      "heading": "Infrastructure as code for data",
      "blocks": [
        {"type": "p", "text": "Every resource that outlives a run belongs in Terraform (or the equivalent): buckets with lifecycle rules, warehouse databases, schemas, roles and grants, orchestrator environments, service identities, secrets, network paths, budgets. The reasons are the DevOps track's: reproducibility, review, drift detection, and one place where an auditor can read how access is granted. Warehouse objects are the part teams forget; Terraform providers exist for the major warehouses, and putting roles and grants there means access review is a diff, exactly as the governance module wanted."},
        {"type": "code", "lang": "hcl", "caption": "A slice of a data platform in Terraform: a lake bucket with lifecycle and encryption, a warehouse schema with role-based grants, and the identity a pipeline runs as.", "code": "resource \"aws_s3_bucket\" \"lake\" {\n  bucket = \"acme-lake-${var.env}\"\n}\n\nresource \"aws_s3_bucket_lifecycle_configuration\" \"lake\" {\n  bucket = aws_s3_bucket.lake.id\n  rule {\n    id     = \"raw-tiering\"\n    status = \"Enabled\"\n    filter { prefix = \"raw/\" }\n    transition { days = 90;  storage_class = \"STANDARD_IA\" }\n    transition { days = 365; storage_class = \"GLACIER_IR\" }\n    expiration { days = 730 }\n  }\n}\n\nresource \"aws_s3_bucket_server_side_encryption_configuration\" \"lake\" {\n  bucket = aws_s3_bucket.lake.id\n  rule { apply_server_side_encryption_by_default { sse_algorithm = \"aws:kms\" } }\n}\n\n# warehouse objects via the Snowflake provider: schema, role, and grants reviewed as code\nresource \"snowflake_schema\" \"marts\" {\n  database = var.warehouse_db\n  name     = \"MARTS\"\n}\n\nresource \"snowflake_account_role\" \"analyst\" { name = \"ANALYST\" }\n\nresource \"snowflake_grant_privileges_to_account_role\" \"analyst_marts\" {\n  account_role_name = snowflake_account_role.analyst.name\n  privileges        = [\"SELECT\"]\n  on_schema_object {\n    future {\n      object_type_plural = \"TABLES\"\n      in_schema          = \"\\\"${var.warehouse_db}\\\".\\\"${snowflake_schema.marts.name}\\\"\"\n    }\n  }\n}\n\n# the identity the orders pipeline runs as: reads raw, writes bronze/silver, nothing else\nresource \"aws_iam_role\" \"orders_pipeline\" {\n  name               = \"orders-pipeline-${var.env}\"\n  assume_role_policy = data.aws_iam_policy_document.orchestrator_assume.json\n}"},
        {"type": "p", "text": "Parameterise by environment (`var.env`) so the same code produces development, staging and production with different names and sizes. Keep secrets out of state and code: Terraform creates the secret container, a pipeline or a human writes the value, and the workload reads it by identity. Run policy checks on the plan (public buckets, wildcard grants, missing encryption) in CI, as the cloud security module described."}
      ]
    },
    {
      "heading": "CI/CD for pipelines and models",
      "blocks": [
        {"type": "p", "text": "Pipeline code and dbt projects are software and deploy like it. A pull request runs unit tests on transformation functions (small fixtures, fast), lints SQL and Python, validates DAG files parse, runs the dbt slim build against a CI schema, and runs the governance and contract checks. Merge to main deploys to staging: the orchestrator picks up the new DAGs (from a synced bucket, a container image or a git sync), dbt artifacts are published, and a smoke run executes on a sample. Promotion to production is a tag or a manual approval, and production runs only what is deployed — nobody edits a DAG on the server."},
        {"type": "code", "lang": "yaml", "caption": "A data repository's CI: tests and lint on every PR, slim dbt build against a CI target, DAG parse check, then deploy on merge.", "code": "name: data-platform\n\non:\n  pull_request:\n  push:\n    branches: [main]\n\npermissions:\n  contents: read\n  id-token: write\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with: {python-version: \"3.12\", cache: pip}\n      - run: pip install -r requirements-dev.txt\n      - run: ruff check . && sqlfluff lint models/ --dialect snowflake\n      - run: pytest tests/unit -q\n      - name: DAGs must parse\n        run: python -c \"from airflow.models import DagBag; b = DagBag('dags', include_examples=False); assert not b.import_errors, b.import_errors\"\n      - name: dbt slim build\n        env:\n          DBT_TARGET: ci\n        run: |\n          dbt deps\n          dbt build --select state:modified+ --state ./prod-artifacts --target ci --fail-fast\n      - run: python scripts/check_governance_tags.py\n\n  deploy_staging:\n    if: github.ref == 'refs/heads/main'\n    needs: test\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: aws-actions/configure-aws-credentials@v4\n        with: {role-to-assume: \"${{ vars.DEPLOY_ROLE }}\", aws-region: eu-west-1}\n      - run: aws s3 sync dags/ \"s3://acme-airflow-staging/dags/\" --delete\n      - run: dbt docs generate --target staging && aws s3 cp target/manifest.json \"s3://acme-dbt-artifacts/staging/manifest.json\"\n\n  deploy_prod:\n    needs: deploy_staging\n    runs-on: ubuntu-latest\n    environment: production            # requires an approval in GitHub before this job runs\n    steps:\n      - uses: actions/checkout@v4\n      - uses: aws-actions/configure-aws-credentials@v4\n        with: {role-to-assume: \"${{ vars.DEPLOY_ROLE_PROD }}\", aws-region: eu-west-1}\n      - run: aws s3 sync dags/ \"s3://acme-airflow-prod/dags/\" --delete"},
        {"type": "p", "text": "Keep the transformation code, the DAGs, the Terraform and the governance tags in one repository per domain, or a small number of them, so a change to a model and to its schedule and to its grants is one reviewed pull request. Version everything, pin dependencies, and store run artifacts (dbt manifests, test results) after production runs for slim CI and observability."}
      ]
    },
    {
      "heading": "Environments for data",
      "blocks": [
        {"type": "p", "text": "Software environments are cheap copies; data environments are not, because the data is the expensive part and copying production data everywhere is a privacy problem. The workable pattern: **development** is a per-developer schema in the warehouse built from a small, masked sample of production (or synthetic data), fast to rebuild, with dbt targets pointing at it. **Staging** mirrors production structure and runs the full pipeline on a recent, masked subset or on production data with restricted access, to catch scale and integration problems. **Production** runs only deployed code, on its own identities and compute, with the SLAs from the observability module. Zero-copy clones and table-format snapshots make staging datasets cheap where the platform supports them."},
        {"type": "list", "items": [
          "Same code, different targets: dbt profiles, orchestrator variables and Terraform `var.env` select the environment; no `if env == 'prod'` branches in logic.",
          "Masked samples for development, generated by a pipeline that applies the governance module's masking rules; refreshed weekly.",
          "Staging is where backfills and migrations are rehearsed before production.",
          "Separate cloud accounts or projects per environment, so a staging mistake cannot touch production data or identities.",
          "Promotion is a deploy, never a manual copy of files or a hand-run notebook."
        ]}
      ]
    },
    {
      "heading": "Cost: attribute, cap, review",
      "blocks": [
        {"type": "p", "text": "Data platforms have three big cost lines: warehouse compute, storage (and the API calls on it), and code compute (Spark clusters, orchestrator workers). The warehouse module covered compute tuning; the platform's job is attribution and control. Tag every resource and query with team and pipeline. Give each domain its own warehouse or reservation so the bill has an owner. Set budgets with alerts at 50, 80 and 100 percent, byte caps on ad hoc access, auto-suspend everywhere, and lifecycle rules on storage. Review the top cost drivers weekly with the owners, and treat an unexplained jump as an incident."},
        {"type": "code", "lang": "hcl", "caption": "A budget with alerts, tagged so the owner is paged rather than finance discovering it at month end.", "code": "resource \"aws_budgets_budget\" \"data_platform\" {\n  name         = \"data-platform-${var.env}\"\n  budget_type  = \"COST\"\n  limit_amount = \"12000\"\n  limit_unit   = \"USD\"\n  time_unit    = \"MONTHLY\"\n\n  cost_filter {\n    name   = \"TagKeyValue\"\n    values = [\"user:platform$data\"]\n  }\n\n  dynamic \"notification\" {\n    for_each = [50, 80, 100]\n    content {\n      comparison_operator        = \"GREATER_THAN\"\n      threshold                  = notification.value\n      threshold_type             = \"PERCENTAGE\"\n      notification_type          = \"ACTUAL\"\n      subscriber_email_addresses = [\"data-platform@acme.example\"]\n    }\n  }\n}"},
        {"type": "callout", "kind": "tip", "text": "The cheapest query is the one a precomputed table answered; the cheapest cluster is the one that suspended; the cheapest data is what retention deleted. Cost control is mostly the disciplines of earlier modules, measured."}
      ]
    }
  ],
  "practice": {
    "title": "Platform as code, deployed by pipeline",
    "steps": [
      "Write Terraform for a small data platform in a test account: a lake bucket with lifecycle and KMS encryption, an orchestrator role, a pipeline role scoped to read raw and write bronze, and a budget with alerts. Use `var.env` and apply it as `dev`.",
      "Add checkov to the plan step and confirm a deliberately public bucket fails.",
      "Create the CI workflow from the lesson for the repository holding your DAGs and dbt project: lint, unit tests, DAG parse check, slim dbt build against a `ci` target, governance tag check.",
      "Add the staging deploy on merge (sync DAGs to a bucket or a folder your local Airflow reads) and a production job gated by a GitHub environment approval. Merge a change and walk it through both.",
      "Build a masked development sample: a pipeline that takes 1% of production-like data, applies hash masking to PII columns per the tags, and writes it to a dev schema. Point your dbt dev target at it.",
      "Tag every resource and the warehouse queries with `team` and `pipeline`; run a cost query grouped by tag after a few runs.",
      "Destroy the dev environment with Terraform and recreate it, timing how long a full rebuild takes. That number is your platform's recoverability."
    ]
  },
  "cheatsheet": {
    "focus": [
      "Components with clear boundaries: ingestion, storage, SQL compute, code compute, orchestration, transformation code, governance, serving",
      "Managed for the stateful hard parts; owned for models, pipelines, contracts, policies",
      "Everything that outlives a run is Terraform, including warehouse schemas, roles and grants; policy checks on the plan",
      "CI: lint, unit tests, DAG parse, slim dbt build, governance checks; CD: staging on merge, production on approval",
      "Environments by target and var.env; masked samples for dev; separate accounts; promotion is a deploy",
      "Cost: tag everything, budgets with alerts, per-domain compute, auto-suspend, lifecycle, weekly review"
    ],
    "groups": [
      {"title": "Terraform for data", "items": [
        {"syntax": "aws_s3_bucket + lifecycle_configuration (IA at 90d, Glacier IR at 365d, expire)", "desc": "Storage tiering as code"},
        {"syntax": "server_side_encryption_configuration sse_algorithm=aws:kms", "desc": "Encryption by default"},
        {"syntax": "snowflake_schema / snowflake_account_role / grant_privileges (future grants)", "desc": "Warehouse objects and access as code"},
        {"syntax": "aws_iam_role per pipeline, assumed by the orchestrator", "desc": "Least-privilege identities"},
        {"syntax": "var.env in every name", "desc": "Same code, three environments"},
        {"syntax": "checkov -d infra/ in CI", "desc": "Policy before apply"}
      ]},
      {"title": "CI/CD", "items": [
        {"syntax": "ruff, sqlfluff lint --dialect X, pytest tests/unit", "desc": "Fast checks on every PR"},
        {"syntax": "DagBag('dags').import_errors == {}", "desc": "DAGs parse"},
        {"syntax": "dbt build --select state:modified+ --state prod-artifacts --target ci", "desc": "Slim CI"},
        {"syntax": "aws s3 sync dags/ s3://.../dags/ --delete", "desc": "Deploy DAGs to a managed orchestrator"},
        {"syntax": "environment: production (approval gate)", "desc": "Promotion needs a human"},
        {"syntax": "store manifest.json + run_results.json after prod runs", "desc": "Artifacts for slim CI and observability"}
      ]},
      {"title": "Environments", "items": [
        {"syntax": "dev: per-developer schema, masked 1% sample, weekly refresh", "desc": "Fast, private, disposable"},
        {"syntax": "staging: prod structure, masked subset or restricted prod data", "desc": "Scale and integration rehearsal"},
        {"syntax": "prod: deployed code only, own identities and compute", "desc": "No edits on the server"},
        {"syntax": "dbt profiles targets: dev / ci / staging / prod", "desc": "Selection by target, not by code branches"},
        {"syntax": "zero-copy clone / table snapshot for staging data", "desc": "Cheap realistic datasets"}
      ]},
      {"title": "Cost", "items": [
        {"syntax": "tags: team, pipeline, env on every resource and query", "desc": "Attribution"},
        {"syntax": "aws_budgets_budget with 50/80/100% notifications", "desc": "Owners find out first"},
        {"syntax": "warehouse per domain; auto-suspend; byte caps", "desc": "Isolation and limits"},
        {"syntax": "lifecycle + partition expiration", "desc": "Storage that shrinks"},
        {"syntax": "weekly top-drivers review with owners", "desc": "Unexplained jump = incident"}
      ]}
    ],
    "pitfalls": [
      "Warehouse roles and grants created by hand in the console, invisible to review and impossible to rebuild.",
      "Editing DAGs directly on the orchestrator server to fix production quickly.",
      "Copying full production data into every developer's schema, multiplying cost and privacy exposure.",
      "`if env == 'prod'` branches in transformation logic, so staging never tests the production path.",
      "One shared warehouse for every team, so nobody owns the bill.",
      "Never rebuilding an environment from code, so the day it must be rebuilt is the day you learn it cannot be."
    ]
  },
  "quiz": [
    {
      "q": "Which of these belongs in Terraform for a data platform, and is most often left out?",
      "options": [
        "Warehouse schemas, roles and grants, so access is reviewed as code and reproducible",
        "The contents of the raw data bucket",
        "The daily run's row counts",
        "The SQL text of every dbt model"
      ],
      "answer": 0,
      "explain": "Warehouse objects and access are infrastructure that outlives runs and should be reproducible and reviewable. Data contents, run metrics and model SQL live elsewhere (storage, telemetry, the dbt repository)."
    },
    {
      "q": "A pull request changes one dbt model and one DAG. What should CI run before it can merge?",
      "options": [
        "Lint, unit tests, a DAG parse check, a slim dbt build of the changed model and its descendants against a CI target, and governance tag checks",
        "A full production dbt run to make sure nothing breaks",
        "Nothing; changes are validated by the nightly production run",
        "A manual review by the platform lead only"
      ],
      "answer": 0,
      "explain": "Fast, targeted automated checks catch most problems before merge. Full production runs in CI are slow and risky; relying on the nightly run means discovering breakage in production."
    },
    {
      "q": "Developers want realistic data to build models against. What is the privacy-conscious, cost-conscious approach?",
      "options": [
        "A pipeline that produces a small masked sample of production per the PII tags, refreshed weekly into per-developer schemas",
        "Granting every developer read access to production tables",
        "Copying the full production warehouse into a shared development database",
        "Having developers work only with hand-written test rows"
      ],
      "answer": 0,
      "explain": "Masked samples give realistic shape and distributions without exposing personal data or paying for full copies. Direct production access and full copies violate least privilege and cost control; tiny hand-written data misses real-world edge cases."
    },
    {
      "q": "A production DAG fails at night and an engineer fixes it by editing the file on the orchestrator server. What is the problem with that?",
      "options": [
        "Production now runs code that is not in version control or CI; the next deploy overwrites the fix and nobody can review or reproduce it",
        "Nothing, as long as the fix works",
        "The orchestrator will refuse to load a modified file",
        "The fix must also be applied to the Terraform state"
      ],
      "answer": 0,
      "explain": "Production must run only deployed, reviewed code. Hotfixes go through the same pipeline (fast-tracked if necessary), or they become invisible drift that the next deploy erases."
    },
    {
      "q": "The monthly warehouse bill doubled and nobody can say why. Which set of controls would have prevented the surprise?",
      "options": [
        "Tags on resources and queries, per-domain compute with owners, budgets with threshold alerts, and a weekly review of top cost drivers",
        "A single large warehouse shared by everyone for simplicity",
        "Disabling auto-suspend so clusters are always warm",
        "Removing byte caps so queries never fail"
      ],
      "answer": 0,
      "explain": "Attribution, ownership and early alerts turn cost into something managed daily rather than discovered monthly. The other options remove the controls that limit spend."
    }
  ]
}
