{
  "id": "blockchain-tooling",
  "minutes": 33,
  "intro": "Writing a contract is only half the job; you also need to compile it, test it exhaustively, and deploy it — and because deployed contracts usually can't be changed and hold real money, the testing matters more here than almost anywhere in software. This lesson introduces the developer toolkit: Hardhat and Foundry, the two dominant frameworks, plus the local blockchain that lets you deploy and experiment instantly and for free. You'll see the develop-test-deploy loop that every serious contract goes through before it touches a real network.",
  "objectives": [
    "Describe what a smart-contract development framework does",
    "Compare Hardhat and Foundry at a practical level",
    "Explain why a local blockchain makes development fast and free",
    "Outline the write → test → deploy workflow and why testing is non-negotiable"
  ],
  "sections": [
    {
      "heading": "What a framework gives you",
      "blocks": [
        {"type": "p", "text": "You could compile and deploy a contract by hand, but no one does, because a **development framework** automates the whole cycle. A framework compiles your Solidity to EVM bytecode, runs your test suite against a simulated blockchain, deploys to whichever network you point it at, and helps you interact with the deployed contract. It also manages dependencies (like pulling in OpenZeppelin) and keeps configuration for different networks in one place. The two you'll hear about constantly are **Hardhat** and **Foundry**; both do these jobs well, and learning the workflow matters more than the choice between them."}
      ]
    },
    {
      "heading": "Hardhat and Foundry",
      "blocks": [
        {"type": "p", "text": "**Hardhat** is a JavaScript/TypeScript framework: you write your tests and deployment scripts in JavaScript, using libraries like ethers.js, which makes it a natural fit if you're also building a web frontend in the same language. It has a rich plugin ecosystem and a built-in local network with helpful features like console logging from inside Solidity. **Foundry** takes a different approach: you write your tests *in Solidity itself*, and it's known for being extremely fast and for powerful testing features like fuzzing (throwing many random inputs at your contract to find edge cases). Many teams use Foundry for its speed and testing depth and reach for Hardhat's JS ecosystem when integrating a frontend; some use both."},
        {"type": "table", "headers": ["Aspect", "Hardhat", "Foundry"], "rows": [["Tests written in", "JavaScript / TypeScript", "Solidity"], ["Best fit", "JS/TS frontends, plugins", "Speed, fuzzing, Solidity-native"], ["Local chain", "Hardhat Network", "Anvil"], ["Known for", "Ecosystem and tooling", "Very fast, strong testing"]]}
      ]
    },
    {
      "heading": "The local blockchain",
      "blocks": [
        {"type": "p", "text": "The single most useful tool is a **local blockchain** — a real EVM running on your own machine (Hardhat Network, or Foundry's Anvil). It starts instantly, gives you a set of accounts pre-funded with fake ether, and mines transactions the moment you send them. This means you can deploy your contract and call its functions hundreds of times a minute, with no waiting and no cost, resetting to a clean state whenever you like. Your tests run against this local chain, so a whole test suite that deploys contracts and exercises them finishes in seconds. Development happens here first; a public testnet comes later, and mainnet last of all."},
        {"type": "code", "lang": "bash", "code": "# A typical Hardhat project, from scratch\nnpm init -y\nnpm install --save-dev hardhat\nnpx hardhat init            # scaffold the project\n\nnpx hardhat compile         # Solidity -> EVM bytecode\nnpx hardhat test            # run tests on the built-in local chain\nnpx hardhat node            # start a standalone local blockchain"}
      ]
    },
    {
      "heading": "Testing is not optional",
      "blocks": [
        {"type": "p", "text": "In ordinary software a bug means a patch. In smart contracts a deployed contract's code usually **cannot be changed**, and it may hold millions in value, so a bug can be an unfixable, catastrophic loss. That inverts the normal priorities: testing is the *main event*, not an afterthought. A good test suite deploys the contract and checks that every function behaves correctly, that permissions are enforced (a non-owner is rejected), that edge cases are handled (zero amounts, empty inputs), and that failures revert as expected. Frameworks make writing these tests straightforward, and you run them constantly as you develop."},
        {"type": "code", "lang": "javascript", "code": "// A Hardhat test (using ethers + chai) for the Counter contract\nconst { expect } = require(\"chai\");\nconst { ethers } = require(\"hardhat\");\n\ndescribe(\"Counter\", function () {\n  it(\"starts at zero and increments\", async function () {\n    const Counter = await ethers.getContractFactory(\"Counter\");\n    const counter = await Counter.deploy();          // deploy to local chain\n    expect(await counter.count()).to.equal(0);       // initial state\n    await counter.increment();                       // send a transaction\n    expect(await counter.count()).to.equal(1);       // state changed\n  });\n});"}
      ]
    },
    {
      "heading": "The develop-test-deploy loop",
      "blocks": [
        {"type": "p", "text": "The whole workflow forms a loop you'll repeat constantly. **Write** or change the contract. **Compile** it to catch type errors. **Test** it against the local chain until everything passes and the edge cases are covered. Only then **deploy** — first to a public **testnet** (the previous stage's free network) to confirm it behaves the same against a real, shared chain, and finally, if it's meant for production, to **mainnet**, ideally after an audit. Each stage is a wider, more expensive net, so you catch problems as early and cheaply as possible. Never skip straight to mainnet; the frameworks make the earlier, safer stages nearly effortless."},
        {"type": "list", "items": [
          "Write / edit the contract.",
          "Compile to catch type and syntax errors.",
          "Test on the local chain until all cases pass.",
          "Deploy to a public testnet and verify real-world behaviour.",
          "Deploy to mainnet last, after thorough testing (and an audit for real value)."
        ]},
        {"type": "callout", "kind": "tip", "text": "A deployment script is code too — keep it in your project and version it, so deploying is reproducible rather than a series of manual clicks. The blockchain projects in this track use exactly this loop: write, test locally, deploy to a testnet."}
      ]
    }
  ],
  "practice": {
    "title": "Run the development loop",
    "steps": [
      "Initialise a Hardhat (or Foundry) project and add a simple contract like Counter.",
      "Compile it and fix any errors, then explain what compilation produces (EVM bytecode).",
      "Start a local blockchain and note the pre-funded test accounts it provides.",
      "Write and run a test that deploys the contract and checks a function changes state as expected.",
      "Describe the full write → test → testnet → mainnet path and why each stage exists."
    ]
  },
  "cheatsheet": {
    "focus": [
      "A framework compiles, tests, deploys, and helps you interact with contracts.",
      "Hardhat uses JS/TS tests; Foundry uses Solidity tests and is very fast with fuzzing.",
      "A local blockchain gives instant, free, pre-funded deployment for development.",
      "Deployed contracts usually can't be changed, so testing is the main event.",
      "Tests check correct behaviour, permissions, edge cases, and expected reverts.",
      "Loop: write → compile → test locally → deploy to testnet → mainnet last."
    ],
    "groups": [
      {"title": "Frameworks", "items": [
        {"syntax": "Hardhat", "desc": "JS/TS framework, rich plugins"},
        {"syntax": "Foundry", "desc": "Solidity tests, fast, fuzzing"},
        {"syntax": "compile", "desc": "Solidity → EVM bytecode"},
        {"syntax": "deploy scripts", "desc": "reproducible, versioned deployment"}
      ]},
      {"title": "Local chain", "items": [
        {"syntax": "Hardhat Network / Anvil", "desc": "a local EVM on your machine"},
        {"syntax": "pre-funded accounts", "desc": "fake ether to spend freely"},
        {"syntax": "instant mining", "desc": "transactions confirm immediately"},
        {"syntax": "resettable", "desc": "clean state on demand"}
      ]},
      {"title": "Testing", "items": [
        {"syntax": "deploy in tests", "desc": "each test can deploy fresh"},
        {"syntax": "check reverts", "desc": "assert failures revert"},
        {"syntax": "permissions", "desc": "non-owner is rejected"},
        {"syntax": "fuzzing", "desc": "many random inputs find edge cases"}
      ]},
      {"title": "The loop", "items": [
        {"syntax": "write", "desc": "edit the contract"},
        {"syntax": "test locally", "desc": "fast, free iteration"},
        {"syntax": "testnet", "desc": "real shared chain, free coins"},
        {"syntax": "mainnet last", "desc": "after tests and an audit"}
      ]}
    ],
    "pitfalls": [
      "Treating tests as optional — a deployed contract's bug is often unfixable and costly.",
      "Skipping the local chain and testing straight on a slow public network.",
      "Deploying to mainnet before testing thoroughly on a testnet.",
      "Only testing the happy path and not the reverts, permissions, and edge cases.",
      "Deploying by hand instead of a versioned script, so deployments aren't reproducible."
    ]
  },
  "quiz": [
    {
      "q": "What does a smart-contract development framework do for you?",
      "options": [
        "It writes the entire contract automatically for you",
        "It compiles, tests, deploys, and helps you interact with contracts",
        "It guarantees your contract can never contain any bug",
        "It hides your contract's source from the whole network"
      ],
      "answer": 1,
      "explain": "Frameworks like Hardhat and Foundry automate the compile-test-deploy cycle and manage dependencies and network config — they don't write or guarantee your code."
    },
    {
      "q": "What is the main difference between Hardhat and Foundry?",
      "options": [
        "Hardhat tests in JavaScript; Foundry tests in Solidity",
        "Only Hardhat can deploy to a real network at all",
        "Foundry does not support testing of any kind",
        "Hardhat works only for ERC-20 token contracts"
      ],
      "answer": 0,
      "explain": "Hardhat uses JS/TS for tests and scripts (good with JS frontends); Foundry writes tests in Solidity and is known for speed and fuzzing. Both compile, test, and deploy."
    },
    {
      "q": "Why is a local blockchain so useful during development?",
      "options": [
        "It permanently stores your contract for the public to see",
        "It deploys instantly and for free with pre-funded accounts",
        "It is the only network that supports smart contracts",
        "It automatically fixes bugs in your contract code"
      ],
      "answer": 1,
      "explain": "A local chain starts instantly, mines transactions immediately, and provides fake-funded accounts, so you can deploy and test hundreds of times for free before touching any public network."
    },
    {
      "q": "Why is testing especially critical for smart contracts?",
      "options": [
        "Testing makes the contract deploy more cheaply",
        "A deployed contract usually can't be changed, so bugs can be catastrophic",
        "The network refuses to run any untested contract",
        "Tests are required before you are allowed to compile"
      ],
      "answer": 1,
      "explain": "Because deployed contract code generally cannot be altered and may hold real value, a bug can mean an unfixable loss — so thorough testing is the main event, not an afterthought."
    },
    {
      "q": "What is the correct order of the deployment path?",
      "options": [
        "Straight to mainnet, then test if problems appear",
        "Local chain, then a public testnet, then mainnet last",
        "Testnet first, then delete it and skip mainnet",
        "Mainnet and local chain at the same time"
      ],
      "answer": 1,
      "explain": "You develop and test on a local chain, then deploy to a free public testnet to confirm real behaviour, and only deploy to mainnet last — each stage a wider, costlier safety net."
    }
  ]
}
