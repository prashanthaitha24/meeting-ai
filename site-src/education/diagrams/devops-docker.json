[
  {
    "id": "docker-multistage",
    "caption": "A multi-stage build: the toolchain lives in a throwaway build stage, only the compiled artifact is copied into a minimal final image, and that small image is what gets pushed and run.",
    "alt": "Source code and the dependency manifest go into a build stage based on a full toolchain image, which produces a binary. Only that binary is copied into a final stage based on a distroless image. The final image is pushed to a registry and run as a container. The build stage is discarded.",
    "place": {"section": 3, "after": 1},
    "groups": [
      {"label": "docker build: only the final stage is kept", "col": 1, "row": 0, "w": 2, "h": 2}
    ],
    "nodes": [
      {"id": "src", "label": "Source", "sub": "code + lockfile", "col": 0, "row": 0},
      {"id": "build", "label": "Build stage", "sub": "golang:1.23, ~800 MB", "col": 1, "row": 0, "kind": "muted"},
      {"id": "bin", "label": "/out/server", "sub": "the artifact", "col": 2, "row": 0, "kind": "accent"},
      {"id": "final", "label": "Final stage", "sub": "distroless, ~10 MB", "col": 2, "row": 1, "kind": "accent"},
      {"id": "reg", "label": "Registry", "sub": "myapp:1.4.0", "col": 3, "row": 1, "kind": "store"},
      {"id": "run", "label": "Container", "sub": "USER nonroot", "col": 3, "row": 2}
    ],
    "edges": [
      {"from": "src", "to": "build", "label": "COPY, RUN go build"},
      {"from": "build", "to": "bin", "label": "produces"},
      {"from": "bin", "to": "final", "label": "COPY --from=build", "label_side": "left"},
      {"from": "final", "to": "reg", "label": "docker push"},
      {"from": "reg", "to": "run", "label": "pull and run"}
    ]
  }
]
