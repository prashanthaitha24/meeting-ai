[
  {
    "id": "dag-run",
    "caption": "One run of the orders DAG for the March 1st interval: a sensor waits for the input, then extract, validate, transform and publish run in dependency order, each one parameterised by the same logical date, retried on transient failure, and delegating heavy work to the warehouse. The reporting DAG starts when the produced dataset updates, not on a guessed time.",
    "alt": "A single DAG run labelled with data interval March 1st. A sensor task waits for the partner file, then extract writes raw data to object storage, validate checks it, transform runs SQL in the warehouse, and publish updates the silver orders dataset. Each task shows its retry policy. An arrow from the dataset to a second DAG, orders reporting, shows data-aware scheduling.",
    "place": {"section": 0, "after": 1},
    "groups": [
      {"id": "run", "label": "orders_daily run: data_interval_start = 2026-03-01", "col": 0, "row": 0, "w": 5, "h": 1}
    ],
    "nodes": [
      {"id": "sensor", "label": "wait_for_file", "sub": "deferrable, timeout", "col": 0, "row": 0, "kind": "muted"},
      {"id": "extract", "label": "extract", "sub": "retries 3, backoff", "col": 1, "row": 0},
      {"id": "validate", "label": "validate_raw", "sub": "retries 0", "col": 2, "row": 0},
      {"id": "transform", "label": "transform", "sub": "SQL in warehouse", "col": 3, "row": 0, "kind": "accent"},
      {"id": "publish", "label": "publish", "sub": "outlet: dataset", "col": 4, "row": 0, "kind": "ok"},
      {"id": "raw", "label": "raw/dt=03-01", "sub": "object storage", "col": 1, "row": 1, "kind": "store"},
      {"id": "wh", "label": "Warehouse", "sub": "does the heavy work", "col": 3, "row": 1, "kind": "store"},
      {"id": "dataset", "label": "silver/orders", "sub": "Dataset updated", "col": 4, "row": 1, "kind": "store"},
      {"id": "report", "label": "orders_reporting", "sub": "schedule=[Dataset]", "col": 4, "row": 2, "kind": "ok"}
    ],
    "edges": [
      {"from": "sensor", "to": "extract", "label": "file present"},
      {"from": "extract", "to": "validate", "label": "path only"},
      {"from": "validate", "to": "transform", "label": "ok"},
      {"from": "transform", "to": "publish"},
      {"from": "extract", "to": "raw", "label": "writes", "route": "straight", "style": "dashed", "color": "muted"},
      {"from": "transform", "to": "wh", "label": "runs", "route": "straight", "style": "dashed", "color": "muted"},
      {"from": "publish", "to": "dataset", "label": "updates", "route": "straight"},
      {"from": "dataset", "to": "report", "label": "triggers", "route": "straight", "color": "ok"}
    ]
  }
]
