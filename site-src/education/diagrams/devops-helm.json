[
  {
    "id": "helm-release",
    "caption": "What `helm upgrade --install` does: values are merged in order of precedence, the templates are rendered into plain manifests, the manifests are applied, and the result is recorded as a numbered revision that a rollback can re-apply.",
    "alt": "Chart templates, the chart's default values, an environment values file and --set flags feed the render step, which produces Kubernetes manifests. The manifests are applied to the API server, and the release is recorded as revision N in a Secret. Rollback re-applies an earlier revision.",
    "place": {"section": 3, "after": 3},
    "groups": [
      {"label": "Inputs (later wins)", "col": 0, "row": 0, "w": 1, "h": 3}
    ],
    "nodes": [
      {"id": "chart", "label": "Chart templates", "sub": "templates/*.yaml", "col": 0, "row": 0},
      {"id": "vals", "label": "values.yaml", "sub": "chart defaults", "col": 0, "row": 1, "kind": "muted"},
      {"id": "envvals", "label": "-f values-prod", "sub": "then --set", "col": 0, "row": 2, "kind": "accent"},
      {"id": "render", "label": "Render", "sub": "Go templates", "col": 1, "row": 1, "kind": "accent"},
      {"id": "manifests", "label": "Manifests", "sub": "plain YAML", "col": 2, "row": 1},
      {"id": "api", "label": "API server", "sub": "apply", "col": 3, "row": 1},
      {"id": "rev", "label": "Revision N", "sub": "Secret in namespace", "col": 3, "row": 2, "kind": "store"}
    ],
    "edges": [
      {"from": "chart", "to": "render", "route": "hv", "offset": -14},
      {"from": "vals", "to": "render", "label": "merged: later wins"},
      {"from": "envvals", "to": "render", "route": "hv", "offset": 14},
      {"from": "render", "to": "manifests", "label": "helm template"},
      {"from": "manifests", "to": "api", "label": "upgrade --install"},
      {"from": "api", "to": "rev", "label": "records"},
      {"from": "rev", "to": "manifests", "label": "helm rollback N", "route": "hv", "style": "dashed", "color": "muted", "offset": 20}
    ]
  }
]
