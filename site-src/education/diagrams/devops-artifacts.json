[
  {
    "id": "supply-chain",
    "caption": "A defended supply chain: pinned dependencies go into a reproducible build, the artifact is scanned and its SBOM recorded, the digest is signed, and the deploy target verifies the signature before it will run anything.",
    "alt": "Source with a lockfile and pinned base image flows into a CI build. The built image is scanned for vulnerabilities and an SBOM is generated and stored. The image digest is signed with cosign and pushed to the registry. At deploy time the cluster verifies the signature and pulls the image by digest.",
    "place": {"section": 4, "after": 3},
    "nodes": [
      {"id": "src", "label": "Source", "sub": "lockfile, pinned base", "col": 0, "row": 0},
      {"id": "build", "label": "CI build", "sub": "reproducible", "col": 1, "row": 0},
      {"id": "scan", "label": "Scan + SBOM", "sub": "trivy, syft", "col": 2, "row": 0, "kind": "accent"},
      {"id": "sign", "label": "Sign digest", "sub": "cosign, OIDC", "col": 3, "row": 0, "kind": "accent"},
      {"id": "sbom", "label": "SBOM store", "sub": "per release", "col": 2, "row": 1, "kind": "store"},
      {"id": "reg", "label": "Registry", "sub": "image@sha256:...", "col": 3, "row": 1, "kind": "store"},
      {"id": "verify", "label": "Verify", "sub": "admission policy", "col": 3, "row": 2, "kind": "ok"},
      {"id": "cluster", "label": "Cluster", "sub": "runs by digest", "col": 2, "row": 2}
    ],
    "edges": [
      {"from": "src", "to": "build"},
      {"from": "build", "to": "scan", "label": "image"},
      {"from": "scan", "to": "sign", "label": "gate passed"},
      {"from": "scan", "to": "sbom", "label": "records", "label_side": "left"},
      {"from": "sign", "to": "reg", "label": "push + signature"},
      {"from": "reg", "to": "verify", "label": "pull"},
      {"from": "verify", "to": "cluster", "label": "signature valid"},
      {"from": "sbom", "to": "cluster", "label": "\"are we affected?\"", "label_side": "left", "style": "dashed", "color": "muted", "dir": "none"}
    ]
  }
]
