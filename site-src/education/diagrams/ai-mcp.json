[
  {
    "id": "mcp-topology",
    "caption": "One host, one client per server: the AI application creates a dedicated client for each MCP server it connects to, talking JSON-RPC over stdio to local servers and over Streamable HTTP to remote ones.",
    "alt": "An MCP host such as an IDE or assistant contains the model and three MCP clients. Client 1 connects over stdio to a local filesystem server. Client 2 connects over stdio to a local database server. Client 3 connects over Streamable HTTP, with OAuth, to a remote ticketing server that many users share. Every connection carries JSON-RPC messages: tools/list and tools/call.",
    "place": {"section": 1, "after": 2},
    "groups": [
      {"label": "MCP host: the AI application", "col": 0, "row": 0, "w": 2, "h": 3},
      {"label": "Local machine", "col": 3, "row": 0, "w": 1, "h": 2},
      {"label": "Remote, shared", "col": 3, "row": 2, "w": 1, "h": 1}
    ],
    "nodes": [
      {"id": "model", "label": "Model", "sub": "sees tool definitions", "col": 0, "row": 1, "kind": "accent"},
      {"id": "c1", "label": "MCP client 1", "col": 1, "row": 0},
      {"id": "c2", "label": "MCP client 2", "col": 1, "row": 1},
      {"id": "c3", "label": "MCP client 3", "col": 1, "row": 2},
      {"id": "s1", "label": "Filesystem", "sub": "server, child process", "col": 3, "row": 0},
      {"id": "s2", "label": "Database", "sub": "server, child process", "col": 3, "row": 1},
      {"id": "s3", "label": "Ticketing", "sub": "server, many users", "col": 3, "row": 2, "kind": "accent"}
    ],
    "edges": [
      {"from": "model", "to": "c1", "label": "tool calls", "route": "vh", "offset": -20},
      {"from": "model", "to": "c2"},
      {"from": "model", "to": "c3", "route": "vh", "offset": 20},
      {"from": "c1", "to": "s1", "label": "stdio, JSON-RPC", "dir": "both"},
      {"from": "c2", "to": "s2", "label": "stdio, JSON-RPC", "dir": "both"},
      {"from": "c3", "to": "s3", "label": "Streamable HTTP + OAuth", "dir": "both", "color": "accent"}
    ]
  }
]
