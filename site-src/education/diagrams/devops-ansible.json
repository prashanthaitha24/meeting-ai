[
  {
    "id": "ansible-push",
    "caption": "Ansible's agentless push model: the control node reads the inventory and the playbook, connects to each managed node over SSH, runs a module there, and collects a JSON result. Nothing is installed on the targets beyond SSH and Python.",
    "alt": "A control node holding the inventory and a playbook connects over SSH to three managed nodes. For each task it copies a small Python module to the node, executes it, and receives a JSON result reporting ok, changed or failed.",
    "place": {"section": 0, "after": 3},
    "groups": [
      {"label": "Control node", "col": 0, "row": 0, "w": 1, "h": 3},
      {"id": "targets", "label": "Managed nodes", "col": 2, "row": 0, "w": 1, "h": 3}
    ],
    "nodes": [
      {"id": "inv", "label": "Inventory", "sub": "groups + vars", "col": 0, "row": 0, "kind": "muted"},
      {"id": "play", "label": "ansible-playbook", "sub": "site.yml", "col": 0, "row": 1, "kind": "accent"},
      {"id": "roles", "label": "Roles, vault", "sub": "reusable, encrypted", "col": 0, "row": 2, "kind": "muted"},
      {"id": "n1", "label": "web1", "sub": "SSH + Python only", "col": 2, "row": 0},
      {"id": "n2", "label": "web2", "sub": "SSH + Python only", "col": 2, "row": 1},
      {"id": "n3", "label": "db1", "sub": "SSH + Python only", "col": 2, "row": 2}
    ],
    "edges": [
      {"from": "inv", "to": "play", "label": "who"},
      {"from": "roles", "to": "play", "label": "what"},
      {"from": "play", "to": "targets", "label": "SSH: copy module, run it", "route": "straight", "offset": -12},
      {"from": "targets", "to": "play", "label": "JSON: ok, changed, failed", "route": "straight", "offset": 12, "style": "dashed", "color": "muted"}
    ]
  }
]
