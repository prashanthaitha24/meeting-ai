[
  {
    "id": "terraform-state",
    "caption": "Why state matters: `plan` compares your configuration with the state file and with what the provider reports, then `apply` makes the API calls and writes the new mapping back to the locked remote state.",
    "alt": "Configuration files and variables go into terraform plan, which reads the remote state file and refreshes real attributes from the cloud provider's API to compute a change set. terraform apply executes the change set through the provider API, creating or changing cloud resources, and writes the new resource mapping back to the remote state, which is locked during the run.",
    "place": {"section": 2, "after": 1},
    "nodes": [
      {"id": "cfg", "label": "Configuration", "sub": "*.tf + tfvars", "col": 0, "row": 0},
      {"id": "plan", "label": "terraform plan", "sub": "+ ~ - -/+", "col": 1, "row": 0, "kind": "accent"},
      {"id": "apply", "label": "terraform apply", "sub": "the saved plan", "col": 2, "row": 0, "kind": "accent"},
      {"id": "provider", "label": "Provider", "sub": "hashicorp/aws", "col": 3, "row": 0},
      {"id": "state", "label": "Remote state", "sub": "locked, encrypted", "col": 2, "row": 1, "kind": "store"},
      {"id": "cloud", "label": "Cloud resources", "sub": "VPC, buckets, DBs", "col": 3, "row": 1}
    ],
    "edges": [
      {"from": "cfg", "to": "plan", "label": "desired"},
      {"from": "plan", "to": "apply", "label": "reviewed change set"},
      {"from": "apply", "to": "provider", "label": "API calls"},
      {"from": "provider", "to": "cloud", "label": "create, change, destroy"},
      {"from": "apply", "to": "state", "label": "writes the new IDs", "label_side": "left"},
      {"from": "state", "to": "plan", "label": "known mapping", "route": "hv", "offset": -20},
      {"from": "cloud", "to": "plan", "label": "refresh: real attributes", "route": "under", "offset": 20, "style": "dashed", "color": "muted"}
    ]
  }
]
