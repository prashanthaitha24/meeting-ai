# Electron App — these are safe to bundle (Supabase anon key is public-safe)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJ...your-anon-key
BACKEND_URL=https://your-backend.vercel.app

# Google OAuth (set up in Supabase dashboard, not needed here)
# Groq key is now on the backend — NOT here
